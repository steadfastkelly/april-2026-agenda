[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — redirect all routes to index.html so
# client-side routing works on page refresh / direct URL access.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
